- content_for :title do
  = t('.title')

.mt-4.mb-5.pb-5
  .container
    .p-4.align-items-center.border.shadow.bg-white.rounded-3
      .row
        .p-3.p-lg-5.pt-lg-0
          .mb-4
            h2 = 'Мои объявления'
            hr
          = link_to 'Добавить объявление', new_bulletin_path, class: 'btn btn-primary mb-3'
          = render 'search_form'
          .row.mt-2
            table.table.table-hover.mt-2
              thead
                tr
                  th{ scope: :col } = sort_link(@q, :title)
                  th{ scope: :col } = 'Состояние'
                  th{ scope: :col } = sort_link(@q, :created_at)
                  th{ scope: :col } Действия
                  th
              tbody
                - @bulletins.each do |bulletin|
                  tr
                    td = bulletin.title
                    td = bulletin.aasm.human_state
                    td = time_ago_in_words(bulletin.created_at)
                    td
                      = link_to 'Show', bulletin_path(bulletin), class: 'me-2'
                      = link_to 'Edit', edit_bulletin_path(bulletin), class: 'me-2'
                      - if bulletin.may_send_to_moderation?
                        = link_to 'На модерацию', send_to_moderation_bulletin_path(bulletin),
                                method: :patch,
                                class: 'me-2'
                      - if bulletin.may_archive?
                        = link_to 'В архив', archive_bulletin_path(bulletin),
                                method: :patch,
                                data: { confirm: 'Are you sure?'},
                                class: 'text-danger me-2'

